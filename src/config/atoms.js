import {atom} from 'jotai';

export const selectedUserAtom = atom(null);
export const disableAtom = atom(false);
export const userNameAtom = atom('@test.com');
export const userPasswordAtom = atom('');
